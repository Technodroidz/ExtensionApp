name: Laravel

on:
  push:
    branches:
      - main

jobs:
  laravel-tests:

    runs-on: ubuntu-latest

    steps:
    - name: Copy .env
      run: php -r "file_exists('.env') || copy('.env.example', '.env');"
